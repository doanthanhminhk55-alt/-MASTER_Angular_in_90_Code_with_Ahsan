import { HightlightCompletedTodo } from './hightlight-completed-todo';

describe('HightlightCompletedTodo', () => {
  it('should create an instance', () => {
    const directive = new HightlightCompletedTodo();
    expect(directive).toBeTruthy();
  });
});
